# myapp
Java web app for Internet Programming class — full CRUD operations on a MySQL database using Servlets, JSP, and JDBC. MVC architecture with manual deployment on Apache Tomcat. No IDE, no build tool — compiled straight from the terminal.
